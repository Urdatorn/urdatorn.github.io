# urdatorn.github.io

Academic homepage built with [Quarto](https://quarto.org/).

Preview locally with:

```sh
quarto preview
```

Pushes to `master` are rendered and deployed to GitHub Pages by GitHub Actions.
